YOU ARE WORKING INSIDE THIS REPO:
C:\Users\ORENS\predictive-log-anomaly-engine

MODE: EXECUTION (implement; do not over-explain)

OBJECTIVE (CLOSE REQUIREMENTS GAP):
Add a minimal, fast Synthetic Data + Scenario pipeline that matches the original requirements
(Stage 1 "Data Layer" + ScenarioBuilder), without retraining and without touching Stage 5 behavior.

NON-NEGOTIABLE:
- NO Docker changes.
- NO dataset downloads.
- NO model training/retraining.
- Keep everything CPU-fast.
- Use pathlib relative paths (repo-relative).
- Must NOT break existing Stage 1–5 scripts.
- If a target file already exists in repo, MODIFY IN PLACE (minimal diff). Do NOT create duplicates.
- Add small tests.

WHAT TO BUILD (MINI but REAL):

1) Create core data model:
   File: src/data/log_event.py
   - LogEvent dataclass with:
     timestamp (datetime), service (str), level (str), message (str),
     meta (dict), label (optional str or int)
   - Provide helpers: to_dict()/from_dict() for parquet IO friendliness.
   - Keep schema stable: timestamp, service, level, message, meta, label.

2) Create Synthetic patterns + generator:
   File: src/data/synth_patterns.py
   - Define FailurePattern (ABC): emit(i, state, rng) -> dict(level,message,meta,label?)
   - Implement 4 patterns (lightweight):
     MemoryLeakPattern
     DiskFullPattern
     AuthBruteForcePattern
     NetworkFlapPattern
   Keep messages realistic and include a stable keyword in meta like {"pattern":"disk_full"}.

   File: src/data/synth_generator.py
   - SyntheticLogGenerator(patterns, seed=42)
   - generate(n_events, services, scenario) -> list[LogEvent]

3) ScenarioBuilder:
   File: src/data/scenario_builder.py
   - Build a sequence of states: normal -> degradation -> failure -> recovery(optional)
   - Control:
     - durations by fractions (e.g., 0.7/0.2/0.1)
     - per-service intensity
   - Ensure labels exist for degradation/failure windows (label=1) and normal label=0
     (labels may be strings, but must be consistent everywhere; default to int 0/1).
   - Output must be list[LogEvent] in a single unified schema.

4) Script to generate + normalize + write parquet:
   TARGET SCRIPT (already exists in repo): scripts/stage_01_synth_generate.py
   - If it exists: update it to implement the required behavior.
   CLI:
     --n-events 50000 (default 50000)
     --services "auth,api,billing,db" (default)
     --seed 42
     --out data/synth/events_synth.parquet (default)
     --schema-out data/synth/schema.md (default)
   Must:
     - create data/synth/ if missing
     - generate events with strictly increasing timestamps (second-level OK)
     - write parquet with columns matching schema exactly:
       timestamp, service, level, message, meta, label
     - write schema.md describing the fields + label meanings
     - write 1-page report:
       reports/stage_01_synth_report.md
       includes: command used, n_events, services, label distribution,
       sample 5 rows as markdown table, and note:
       "synthetic demo generator for requirement coverage"

5) Connect to existing Stage 2/3 quickly (NO refactor):
   Add a tiny helper script:
   File: scripts/stage_01_synth_to_processed.py
   CLI:
     --in  data/synth/events_synth.parquet
     --out data/processed/events_synth.parquet
   Purpose:
     - ensure it matches EXACT same schema conventions used by existing pipeline.
     - minimal: load parquet and re-save (and enforce sorting by timestamp).
   DO NOT modify existing data/processed/events_tokenized.parquet.

   NOTE:
   - If there is already a Stage 2 tokenize script that can accept input path,
     you MAY add optional flags --input-parquet and --output-parquet WITHOUT breaking defaults.
   - Otherwise DO NOT attempt to tokenize synth now — just ensure Stage 1 deliverables exist.

6) Deliverables required (must exist after running):
   - data/synth/events_synth.parquet
   - data/synth/schema.md
   - data/processed/events_synth.parquet
   - reports/stage_01_synth_report.md (1 page; generated by stage_01_synth_generate.py)

7) Logging:
   - Log file: ai_workspace/logs/stage_01_generate_synth.log
   Requirements:
   - Use Python logging with FileHandler + StreamHandler
   - Include throughput (events/sec) and memory if psutil available
   - Ensure directories exist before writing logs

8) Minimal tests:
   TARGET TEST (already exists in repo): tests/unit/test_synth_generation.py
   - If it exists: update it to match these assertions (minimal diff).
   Test must:
   - Run generator with n_events=2000 and 2 services
   - Assert parquet written and non-empty
   - Assert required columns exist
   - Assert label contains at least 2 classes (normal + anomaly) in the generated output

VERIFICATION STEPS (MUST RUN AND REPORT RESULTS):
- pytest -q
- python scripts/stage_01_synth_generate.py --n-events 50000
- python scripts/stage_01_synth_to_processed.py --in data/synth/events_synth.parquet --out data/processed/events_synth.parquet
- Confirm output files exist and are non-empty.

FINALLY:
Print exact PowerShell commands (copy-paste ready) for:
1) pip install -r requirements.txt
2) pytest -q
3) python scripts/stage_01_synth_generate.py --n-events 50000
4) python scripts/stage_01_synth_to_processed.py --in data/synth/events_synth.parquet --out data/processed/events_synth.parquet

And print the confirmed paths:
- data/synth/events_synth.parquet
- data/synth/schema.md
- data/processed/events_synth.parquet
- reports/stage_01_synth_report.md
- ai_workspace/logs/stage_01_generate_synth.log

BEGIN NOW.
